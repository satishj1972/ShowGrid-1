name: showgrid
description: "ShowGrid - Talent Competition Platform"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router: ^14.2.0
  flutter_riverpod: ^2.5.1
  camera: ^0.10.5+9
  permission_handler: ^11.3.1
  image_picker: ^1.1.2
  path_provider: ^2.1.3
  flutter_image_compress: ^2.3.0
  
  # Firebase
  firebase_core: ^3.1.0
  firebase_auth: ^5.1.0
  cloud_firestore: ^5.0.0
  firebase_storage: ^12.1.0
  cloud_functions: ^5.1.0
  google_sign_in: ^6.2.1
  
  # Utilities
  http: ^1.2.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
